# Server
server.port=8080

# App Database (Local MySQL - metadata store)
spring.datasource.url=jdbc:mysql://mysqlhcubeprd01.mysql.database.azure.com:3306/smartmigrate?useSSL=true&requireSSL=true
spring.datasource.username=hashadmin
spring.datasource.password=M$rus$%^2026!#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
app.jwt.secret=SmartMigrate_JWT_Secret_Key_2024_MustBe256BitsLong_ChangeInProd!
app.jwt.expiration-ms=86400000

# Static Auth
app.auth.username=admin
app.auth.password=admin123

# QuickBooks OAuth
app.quickbooks.client-id=YOUR_QB_CLIENT_ID
app.quickbooks.client-secret=YOUR_QB_CLIENT_SECRET
app.quickbooks.redirect-uri=http://localhost:8080/api/oauth/quickbooks/callback
app.quickbooks.environment=sandbox

# Async thread pool
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=10

# Logging
logging.level.com.smartmigrate=INFO
logging.level.org.springframework.security=WARN
